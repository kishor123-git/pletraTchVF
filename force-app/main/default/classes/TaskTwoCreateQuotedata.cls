public with sharing class TaskTwoCreateQuotedata {
    @AuraEnabled
    public static void getRecordopp(List<Opportunity> opportunitydata, List<OpportunityLineItem> opportunityLine){
        try {
            
        } catch (Exception e) {
            throw new AuraHandledException(e.getMessage());
        }
    }
}